# Live Progresso (same project as progresso.rritjesade.com)
SUPABASE_URL=https://dtigibuhzhmxrkfzvijt.supabase.co
SUPABASE_ANON_KEY=sb_publishable_OWFhyB71KpmUw3Jdzk2MGw_ie6EZgys

# Preferred: long-lived refresh token from a signed-in Progresso session
# (Mac Keychain supabase-session → refreshToken, or browser session).
PROGRESSO_REFRESH_TOKEN=

# Optional bootstrap / fallback access JWT (expires). Used with refresh when both set.
PROGRESSO_ACCESS_TOKEN=

# NEVER use the service-role key.
